# Daily English · 江西自考冲刺台——项目重建提示词

下面的内容可以直接交给 AI 编程工具，用于从零重建本项目。它描述的是产品目的、用户需求、核心功能、数据边界、技术约束和验收标准，而不是只描述当前页面长什么样。

---

## 可直接使用的完整提示词

你是一名资深产品设计师、前端工程师和学习系统设计者。请从零设计并实现一个名为 **“Daily English · 江西自考冲刺台”** 的本地 Web 应用。不要只输出方案、原型或伪代码，需要真正创建完整项目、写入文件、运行构建与测试，并修复发现的问题。

### 一、项目背景与产品目的

这是一个面向中国在职成年人的英语自学网站。目标用户目前英语水平大约相当于大学英语四级，平时在金融公司的信息科技部门工作，工作日可用于学习的时间有限，同时正在准备江西省高等教育自学考试本科阶段的 `13000 英语（专升本）`，距离考试大约三个月。

用户不喜欢传统网课式的连续讲解，也不希望把学习过程变成机械背单词。他更喜欢游戏、二次元、足球、科技和金融科技等内容。因此，这个产品既要帮助用户完成自考英语备考，也要通过感兴趣的主题维持长期学习动力。

产品的核心目的不是“展示很多英语资料”，而是每天帮助用户完成一个小而完整的学习闭环：

1. 回忆之前学过的表达；
2. 阅读一篇难度合适的短文；
3. 完成少量理解、翻译或语言练习；
4. 用英语输出真实想法；
5. 记录错误和学习结果；
6. 根据记忆情况安排下一次复习。

产品需要让学习进度可见、让每天的任务足够小、让用户下班后仍愿意打开。界面文案应传达“稳定比爆发重要”“今天只完成一个小目标”，避免制造焦虑。

### 二、必须达成的产品目标

实现一个真正可使用的个人英语学习工作台，而不是静态展示页面。至少达成以下目标：

- 用户打开首页后，能立即知道距离考试还有多少天、今天需要做什么、预计需要多少分钟。
- 用户能够勾选每日任务，完成状态和学习数据在刷新页面后仍然存在。
- 用户能够阅读原创英文材料，并按需显示中文翻译，不强制一开始就展示中文。
- 用户能够听取英文段落朗读；优先使用浏览器原生 `SpeechSynthesis`，不可用时给出友好提示。
- 用户能够复习实用表达，先回忆再显示答案，并根据掌握程度安排下一次复习。
- 用户能够记录、分类和删除错题或易错表达。
- 用户能够查看任务完成率、复习数量、错题数量、连续学习天数和阶段性成绩趋势。
- 用户能够导出全部本地学习数据，也能够从备份文件恢复，避免更换浏览器或清理缓存后永久丢失。
- 应用在常见桌面浏览器和手机浏览器中都能正常使用。
- 页面直接访问、刷新和客户端水合后都不能出现空白页；不得依赖桌面应用或 `file://` 路径。

### 三、平台范围与明确的非目标

本项目 **只支持 Web 端**，本地运行即可。

不要实现以下内容：

- 不要使用 Electron、Tauri 或任何 Windows 桌面打包方案。
- 不要生成 Windows 安装包、桌面快捷方式或桌面专用代码。
- 第一版不要强制登录、注册、云同步或服务器数据库。
- 不要要求用户连接第三方账号才能使用核心功能。
- 不要复制或分发受版权保护的教材全文、整套未授权真题、答案或网课讲义。
- 不要把页面做成只有图表和假数字的静态仪表盘。
- 不要声称能够预测真题或保证考试通过。

第一版的数据保存在当前浏览器。未来可以扩展账户和云同步，但不能让这些扩展破坏本地优先的使用方式。

### 四、用户画像与学习节奏

主要用户画像：

- 中国在职成年人；
- 英语水平约 CET-4，阅读基础尚可，但输出、长难句、语法和持续学习能力需要加强；
- 工作日晚上学习，注意力和精力有限；
- 不追求考试之外的学术英语，希望英语能够连接兴趣和工作；
- 备考目标为江西省自考本科 `13000 英语（专升本）`；
- 喜欢游戏、二次元、足球、科技、金融科技；
- 不喜欢大段说教、机械抄写和孤立背单词。

默认学习节奏：

- 工作日约 75 分钟；
- 周末约 120 分钟；
- 每天以 4 个任务组成一个闭环；
- 允许用户只完成部分任务，不能因为没有全部完成而清空已有记录。

默认每日任务可以采用：

1. 复习记忆队列，15 分钟；
2. 完成一篇基础阅读，25 分钟；
3. 完成少量理解、翻译或语法练习，20 分钟；
4. 完成 5 句左右的英语输出，15 分钟。

### 五、核心页面与功能需求

整个产品可以使用单页应用式工作台，也可以拆分路由，但必须保持导航清晰。至少包含以下五个主要模块。

#### 1. 今日作战

首页是用户每天最常使用的页面，需要包含：

- 当前日期和星期；
- 课程名称 `13000 英语（专升本）`；
- 距离考试日期的动态倒计时；
- 当前所处的 12 周阶段；
- 今日完成百分比；
- 已完成任务数、任务总数和剩余预计时间；
- 今日任务列表，每项可点击完成或取消完成；
- 一条简短、克制的学习提示；
- 快速进入今天课程和记忆复习的入口；
- 手动重置今日任务的按钮，操作后给出反馈。

每日任务必须按日期保存。进入新的一天时，新的任务状态应自动初始化，同时保留历史完成记录。不要让同一组任务永久停留在某一天的状态。

#### 2. 课程阅读

课程页用于完成兴趣与考试基础结合的原创阅读。每节课程至少包含：

- 英文标题和中文副标题；
- 主题标签、难度标签和预计用时；
- 3 至 6 个英文段落；
- 可切换的中文翻译，默认隐藏；
- 每段英文的朗读按钮；
- 5 个左右的重点词块或句型，提供中文含义和自然例句；
- 3 至 5 道理解、翻译或思考题；
- 一个简短输出任务，例如用 5 句英文表达自己的学习计划或观点；
- “标记课程完成”按钮，并同步更新今日任务。

课程难度以 CET-4 到中级英语为基准。不要把文章写成考试说明书，优先使用游戏、二次元、足球、科技、金融科技和职场生活等用户感兴趣的话题，同时自然覆盖阅读理解、长难句、翻译和写作能力。

每篇内容应明确标注来源类型：

- `original`：原创练习；
- `official`：官方公开信息；
- `user-provided`：用户合法提供的资料；
- `public-web`：公开网页材料的标题、链接或合规短摘录。

#### 3. 记忆复习

复习模块使用轻量级间隔复习，不要求用户机械背词。复习单位优先是“表达、词块或句型”，不是孤立单词。

交互流程：

1. 卡片正面只显示英文表达；
2. 用户先尝试回忆；
3. 点击后显示中文含义和例句；
4. 用户选择掌握程度；
5. 系统计算下次复习日期并保存。

最低限度支持：

- “再来一次”：1 天后；
- “还不错”：3 天后；
- “很熟”：7 天后；
- 可以预留“已掌握”：14 天后。

系统每次打开时，需要把 `due <= 今天` 的卡片放入待复习队列，不能只识别字符串 `today`。日期计算必须使用用户本地日期，避免 UTC 时区导致凌晨日期错误。

初始表达可以包括：

- `set a goal`：设定目标；
- `be more effective`：更加有效；
- `instead of doing...`：而不是做……；
- `turn A into B`：把 A 变成 B；
- `be difficult to continue`：难以继续。

#### 4. 错题本

错题本用于记录真正困扰用户的内容，支持：

- 输入一道错题、错误表达、长难句或错误原因；
- 按 Enter 或点击按钮保存；
- 选择分类：阅读理解、词汇语法、翻译、写作、长难句；
- 自动记录本地日期；
- 显示所有历史记录；
- 删除单条记录，避免列表只能增加不能维护；
- 列表为空时显示清晰的空状态说明；
- 所有记录刷新后仍然存在。

如时间允许，可以进一步加入“错误原因”“正确思路”“下次复习日期”，但不要牺牲基础功能的可靠性。

#### 5. 学习统计

统计页用于让进步可见，至少显示：

- 今天完成了几项任务；
- 今日完成率；
- 当前总记忆卡数量和今日待复习数量；
- 错题记录数量；
- 连续学习天数；
- 距离考试天数；
- 最近若干次模拟成绩趋势。

不要长期使用没有来源的固定假数据冒充真实记录。可以在首次运行时提供明确标记为示例的模拟分数，但用户新增数据后应以真实记录替代。连续学习天数需要基于实际学习日期计算，而不是永远固定为 1。

### 六、备份、恢复与本地记忆

使用浏览器本地存储保存个人数据。可以采用 `localStorage`，默认键名使用：

`jiangxi-english-study-state-v1`

建议的状态结构：

```ts
type DailyTask = {
  id: string;
  date: string;
  label: string;
  meta: string;
  duration: number;
  done: boolean;
  completedAt?: string;
};

type MemoryItem = {
  id: string;
  phrase: string;
  meaning: string;
  example: string;
  due: string;
  interval: number;
  reviewCount: number;
  lastReviewedAt?: string;
};

type Mistake = {
  id: string;
  text: string;
  type: string;
  date: string;
  reason?: string;
  correction?: string;
};

type StudySession = {
  date: string;
  completedTaskIds: string[];
  minutes: number;
};

type AppState = {
  schemaVersion: number;
  dailyTasks: Record<string, DailyTask[]>;
  memory: MemoryItem[];
  mistakes: Mistake[];
  sessions: StudySession[];
  mockScores: Array<{ date: string; score: number }>;
  lastStudyDate?: string;
};
```

实现要求：

- SSR 阶段不能直接访问 `window` 或 `localStorage`；
- 客户端挂载后再读取数据；
- 解析失败时保留安全默认值，不能导致整个页面崩溃；
- 对导入的 JSON 做结构验证，不能盲目信任文件内容；
- 导出文件名使用 `jiangxi-english-study-backup.json`，内容包含版本号和导出时间；
- 导入前给出确认提示，导入成功后立即刷新界面；
- 为未来状态结构变化保留简单的数据迁移能力。

### 七、考试与计划数据

考试和计划数据应放在独立文件中，作为页面的单一数据源，不要在多个组件中重复硬编码。

建议目录：

```text
public/data/
  exam_profile.json
  12_week_plan.json
  content_index.json
  2026-08-01_foundation_reading.md
```

初始考试资料：

- 地区：江西省；
- 考试年份：2026；
- 考试场次：下半年；
- 考试日期：2026 年 10 月 24 日至 25 日；
- 当前课程代码：13000；
- 当前课程名：英语（专升本）；
- 旧课程代码：00015；
- 旧课程名：英语（二）；
- 学分：7；
- 考试方式：笔试；
- 教材：《英语（二）自学教程》；
- 作者：张敬源、张虹；
- 出版社：外语教学与研究出版社；
- 版本：2012 年版。

这些信息需要在数据文件中附带来源标题、来源 URL、来源级别和核验提醒。页面应提醒用户最终以个人准考证、报名系统和所属专业考试计划为准。

12 周计划至少包含：

1. 第 1 周：摸底与建档；
2. 第 2 至 5 周：教材第一轮与基础补强；
3. 第 6 至 8 周：题型专项；
4. 第 9 至 10 周：整套真题训练；
5. 第 11 至 12 周：冲刺与保持。

页面不必一次展示计划中的所有文字，但首页和统计页应能显示当前阶段，并为后续课程调度保留结构。

### 八、初始原创课程

至少内置一节原创基础阅读：

- ID：`foundation-reading-001`；
- 英文标题：`Small Improvements Are Easier to Keep`；
- 中文标题：`为什么小进步更容易坚持`；
- 难度：CET-4 到中级；
- 技能：阅读、语法、翻译、写作；
- 来源类型：`original`；
- 核心观点：成年学习者常因目标过大而放弃；较小、可重复、可记录的行动更容易坚持；技术可以帮助安排复习和显示进度，但不能替代学习者主动阅读、回忆和表达。

文章需要配套中文翻译、重点表达、理解题和 5 句英文输出任务。后续课程应能通过内容索引继续扩展，而不是全部硬编码在一个巨大组件中。

兴趣课程的推荐主题方向：

- 游戏世界观、剧情叙事、角色塑造和玩家体验；
- 二次元作品中的成长、选择和团队关系；
- 足球比赛、战术、球员心态和俱乐部科技；
- 人工智能、软件工程、网络安全和消费科技；
- 金融科技、支付系统、数据质量、自动化和信息科技部门的日常工作。

兴趣主题只是材料载体，学习目标仍要服务于阅读理解、翻译、语法、写作和真实表达。

### 九、视觉与交互设计

整体视觉应成熟、安静、具有学习仪式感，适合成年人长期使用，不要做成儿童背词软件。

建议视觉方向：

- 深墨绿色或深青绿色侧边栏；
- 米白、浅灰绿色主背景；
- 白色卡片；
- 翡翠绿色作为完成、进度和主要按钮颜色；
- 琥珀色用于提醒、记忆卡和考试倒计时；
- 标题使用现代无衬线字体，英文长文可使用易读的衬线字体；
- 圆角适中，阴影克制，信息层级清晰；
- 可以使用 CSS 图形和简洁图标，不依赖大图素材。

桌面端布局：

- 左侧固定或稳定的导航区域；
- 右侧为主要内容；
- 首页上方突出今日目标和进度；
- 内容宽度不能过宽，长文保持舒适行长。

移动端布局：

- 导航改为顶部或横向可滚动区域；
- 内容单列展示；
- 不出现横向溢出；
- 任务按钮和评分按钮适合触摸；
- 隐藏次要装饰，但不能隐藏核心功能。

交互要求：

- 所有按钮必须真的可用；
- 完成任务、添加错题、安排复习、导出数据后显示短暂反馈；
- 键盘用户能够操作主要控件；
- 使用语义化 HTML 和清晰的 `aria-label`；
- 焦点状态明显；
- 尊重 `prefers-reduced-motion`；
- 动画只用于反馈，不妨碍阅读。

### 十、技术要求

优先沿用以下技术栈：

- React 19；
- TypeScript；
- Next.js/Vinext 应用结构；
- Vite 构建；
- 原生 CSS 或项目现有样式体系；
- Node.js `>=22.13.0`。

应保留并确保以下命令有效：

```bash
npm ci
npm run dev
npm run build
npm run test
npm run lint
```

工程要求：

- Web-only，依赖中不得出现 Electron 或桌面打包工具；
- 不要引入没有实际用途的大型依赖；
- 核心数据和内容与 UI 组件分离；
- 复杂页面合理拆分组件，不把全部业务永久堆在单一文件；
- 所有日期使用明确格式，并考虑中国时区和本地日期；
- 首次服务端渲染与客户端挂载内容要稳定，避免 hydration 错误；
- 加入错误边界或可靠的异常降级，数据损坏时也应显示可恢复界面；
- 不需要后端即可完成全部核心功能；
- 不得将密钥、个人信息或本地绝对路径写入前端代码。

### 十一、版权与数据边界

项目只能直接包含：

- 官方考试信息及其来源链接；
- 用户合法提供的个人资料和笔记；
- 原创学习材料；
- 公开网页的标题、URL、元数据和合规短摘录。

不得直接包含：

- 未经授权的教材全文；
- 批量下载或重新分发的教材 PDF；
- 未经授权的整套真题、答案和课程讲义；
- 声称“押中原题”的内容；
- 没有来源、日期和版权说明的外部材料。

每条外部资料建议保存：

```text
sourceType
sourceTitle
sourceUrl
sourceDate
courseCode
licenseNote
confidence
```

### 十二、验收标准

完成项目后必须逐项验证：

1. `npm ci` 能够正常安装依赖；
2. `npm run dev` 能启动本地 Web 服务；
3. 浏览器打开本地地址后能看到完整页面，不是空白页；
4. 浏览器控制台没有导致功能失效的错误；
5. `npm run build` 成功；
6. `npm run test` 成功；
7. `npm run lint` 成功；
8. 首页、课程、复习、错题和统计五个模块都能切换；
9. 勾选任务后刷新页面，状态仍然存在；
10. 第二天打开时生成新一天的任务，同时保留昨天的历史；
11. 中文翻译显示/隐藏正常；
12. 浏览器支持时英文朗读正常，不支持时不会崩溃；
13. 复习评分会更新正确的下次复习日期；
14. 到期日期早于或等于今天的卡片都会进入队列；
15. 添加和删除错题后刷新页面，结果仍然正确；
16. 数据可以成功导出为 JSON；
17. 合法备份可以重新导入并恢复状态；
18. 非法或损坏的备份文件不会导致白屏；
19. 页面在宽屏、平板和手机宽度下都无明显布局问题；
20. `package.json` 中不存在 Electron、Windows 安装包或桌面打包脚本。

### 十三、最终交付物

完成后交付：

- 可本地运行的完整 Web 项目；
- 清晰的 `README.md`；
- `public/data/` 下的考试、计划和初始课程数据；
- 本地状态、复习、错题、统计、备份和恢复功能；
- 必要的自动化测试；
- 构建和测试结果说明；
- 简短列出已实现功能、已知限制和后续扩展方向。

实施时请优先保证核心闭环可靠，再处理视觉细节。如果现有目录中已有考试数据和原创课程，应复用并保留；如果文件不存在，则根据本提示词创建结构化的初始数据。遇到非关键细节缺失时使用合理默认值继续实现，不要只停留在提问或开发计划阶段。

